@model List<ResultContactDto>
@{
    ViewData["Title"] = "Inbox";
    Layout = "~/Views/MvcLayout/AdminLayout.cshtml";
}

<div class="email-left-box">
    <a href="/AdminContact/CreateSendMessage/" class="btn btn-primary btn-block">Yeni Mesaj Oluştur</a>
    @await Html.PartialAsync("SideBarAdminContactPartial")
    @await Html.PartialAsync("SideBarAdminCategoryPartial")
</div>
<div class="email-right-box">
    <div role="toolbar" class="toolbar">
        <div class="btn-group">
            <button aria-expanded="false" data-toggle="dropdown" class="btn btn-dark dropdown-toggle" type="button">
                Gelen Mesajlar <span class="caret m-l-5"></span>
            </button>
        </div>
    </div>
    <div class="email-list m-t-15">
        @foreach (var item in Model)
        {
            <div class="message">
                <a href="/AdminContact/MessageDetails/@item.ContactID">
                    <div class="col-mail col-mail-1">
                        <div class="email-checkbox">
                            <input type="checkbox" id="chk2">
                            <label class="toggle" for="chk2"></label>
                        </div><span class="star-toggle ti-star"></span>
                    </div>
                    <div class="col-mail col-mail-2">
                        <div class="subject">@item.Message</div>
                        <div class="date">@item.Date.ToShortDateString()</div>
                    </div>
                </a>
            </div>
        }
    </div>
    <!-- panel -->
</div>


